node_modules
.next
.env
.env.local
data/submissions.local.json
*.log
.DS_Store
data/users.local.json
data/access_log.local.json

PRODUCTION-SECRETS.local.txt

vercel-import.env

# local dev data — never commit anything under data/ (real records live in the
# database, these are local working/seed copies and can contain PHI)
data/*.local.json
data/*.json
.vercel
.env*

# local dev note (worktree-specific)
BILLING-WORKSPACE.md
TASKS.md
dashboard.html

# stray data / scratch (never commit PHI or design zips)
db/*.json
*.zip
HANDOFF-*.md
